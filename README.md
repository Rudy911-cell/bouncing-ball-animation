# Ball Animation

A simple bouncing ball animation built using HTML, CSS, and JavaScript.

## Features
- CSS keyframe animation
- JavaScript uses `setInterval()` to start and stop the animation
- Beginner-friendly project
